import styled from 'styled-components';

const SCarsView = styled.div`
  margin: 150px 20%;
  display: flex;
  flex-direction: row;
  justify-content: center;
  flex-wrap: wrap;
`;

export default SCarsView;
